"""Domain models for LOGOSFORGE.

Modules listed here are imported so SQLModel metadata is populated before
`SQLModel.metadata.create_all` is invoked. Domain entities will be added in
later iterations.
"""
